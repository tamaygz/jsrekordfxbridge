# Sweep effect YAML
name: sweep
description: "Color sweep across all lights"
steps:
  - action:
      type: fade
      color: { r: 0, g: 128, b: 255 }
      intensity: 1.0
    duration: 1500
    target:
      type: all
      selector: all